# Tests

Reserve this root for cross-runtime fixtures and real desktop E2E tests. Keep feature unit tests near their JavaScript or Rust owners.

Add commands to `docs/quality.md` only after tooling exists and passes.
