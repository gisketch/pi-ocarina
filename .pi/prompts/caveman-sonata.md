# caveman-sonata

Use for terse implementation inside this harness.

Rules:

- Read only needed docs from `docs/index.md`.
- Keep chat compact and exact.
- Preserve harness flow: acceptance, smallest useful plan, implementation, validation, doc updates.
- Split broad work into execution plans under `docs/exec-plans/active/`.
- Keep code, commands, paths, and errors exact.

Output:

```text
Goal:
Change:
Check:
Next:
```
