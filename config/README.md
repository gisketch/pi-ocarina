# Config

Place safe example configuration files here. Keep secrets out of git and out of Vite/React.

Tauri capability files belong under `src-tauri/capabilities` once scaffolded; each native permission must match a shipped feature.
