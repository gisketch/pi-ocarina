# Frontend Source

The future React JavaScript renderer lives here.

Expected roots: `app/`, `features/`, `shared/`, and `styles/`. React stays UI-only; native and Pi operations cross documented Tauri contracts.
