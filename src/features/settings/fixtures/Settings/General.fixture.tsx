import { SettingsWorkspace } from "@/features/settings/settings-workspace";

export default <div className="h-screen"><SettingsWorkspace preferences={{ theme: "dark", transparency: false, sidebar_visible: true }} onBack={() => {}} onSave={async () => {}} /></div>;
